  修改内容：

  - services/account_service.py
      - 新增 get_image_quota_summary()，返回 total、available_accounts、available_quota
  - api/accounts.py
      - 新增 GET /api/accounts/quota
      - 使用 require_identity(...)，管理员和普通用户都可访问
  - web/src/lib/api.ts
      - 新增 AccountQuotaSummary
      - 新增 fetchAccountQuotaSummary()
  - web/src/app/image/page.tsx
      - 图片首页额度改为调用 /api/accounts/quota
      - 移除了普通用户直接显示 -- 的逻辑
      - ImagePageContent 不再接收 isAdmin
	  
	  
	  
	  
	  2.
	    改动点：

  - api/support.py:49
      - resolve_image_base_url() 现在优先读取 Railway/反代常用的：
          - X-Forwarded-Proto
          - X-Forwarded-Host
      - 如果请求实际是 HTTPS，即使配置里的 base_url 是 http://...，也会返回 https://...
  - web/src/lib/image-url.ts:1
      - 新增 normalizeBrowserImageUrl()
      - HTTPS 页面里遇到同域名的 http://.../images/... 会自动改成 https://...
  - web/src/app/image/page.tsx:115
      - “加入编辑”读取历史图片时会先修正 URL，再 fetch
  - web/src/app/image/components/image-results.tsx:34
      - 图片结果显示和下载也会修正旧的 http 图片 URL
  - web/src/components/image-thumbnail.tsx:25
      - 缩略图显示也做同样修正
  - web/src/app/image-manager/page.tsx:86
      - 图片管理页的预览/灯箱/删除确认图也修正旧 URL

  我也确认了没有留下 web/node_modules 或 package-lock.json。


3.
• 已进一步加固这次 Mixed Content 修复。

  这次补的关键点：

  - 后端 api/support.py:49
      - 支持 Forwarded: proto=https
      - 如果 host 是 .up.railway.app，强制生成 https://...
      - 即使设置里的 base_url 是 http://...，在 HTTPS 请求下也会自动改成 https://...
  - 前端历史数据 web/src/store/image-conversations.ts:69
      - 从 IndexedDB 读取旧图片历史时，会把同域名 http://.../images/... 自动修正成 https://...
  - 前端图片页 web/src/app/image/page.tsx:162
      - 新生成任务返回的 first.url 写入历史前就会修正
      - 点击“加入编辑”时，会先把传入图片对象里的 URL 修正，再 fetch

  这次处理后，已经保存过的旧 http://chatgpt2api-production-1fcf.up.railway.app/images/... 也会在页面加载和点击“加入编辑
  ”时转成 HTTPS。

  部署到 Railway 后建议浏览器硬刷新一次，避免旧 JS chunk 还在缓存里。
  
  
  
  4.
  • 已加上“普通用户生图次数限制”。

  实现方式：

  - 管理员在“用户密钥管理”里创建/编辑用户密钥时，可以填写 生图次数限制
      - 留空：不限制
      - 填 2：该用户最多提交 2 次生图任务
  - 用户密钥列表会显示：
      - 总限制
      - 已用次数
      - 剩余次数
  - 编辑用户密钥时也可以调整“已用次数”，比如重置为 0

  后端限制点也加好了：

  - 普通用户每创建一个图片任务扣 1 次
  - 管理员不受限制
  - 次数用完后返回 429，错误信息是 生图次数已用完
  - 同时覆盖了：
      - /api/image-tasks/generations
      - /api/image-tasks/edits
      - /v1/images/generations
      - /v1/images/edits

  修改文件：

  - services/auth_service.py
  - services/image_task_service.py
  - api/accounts.py
  - api/image_tasks.py
  - api/ai.py
  - web/src/lib/api.ts
  - web/src/app/settings/components/user-keys-card.tsx
  
  
  
  5.
  • 已改成“用户页面剩余额度 = 当前用户自己的生图次数限制剩余值”。

  具体行为：

  - 普通用户登录图片页：
      - 管理员给该用户密钥设置 2
      - 页面显示剩余额度 2
      - 用户每提交 1 个生图任务扣 1
      - 用完后显示 0，继续生图会被后端拒绝
  - 管理员登录图片页：
      - 仍显示号池可用额度
  - 如果某个普通用户没有设置生图次数限制：
      - 图片页显示 不限

  改动文件：

  - api/accounts.py
      - /api/accounts/quota 现在会根据当前登录身份返回：
          - scope: "user" | "pool"
          - display_quota
          - image_quota_limit
          - image_quota_used
          - image_quota_remaining
  - web/src/lib/api.ts
      - 扩展了 AccountQuotaSummary 类型
  - web/src/app/image/page.tsx
      - 普通用户显示 display_quota
      - 管理员显示号池额度
	  
	  6.
	  
	  • 已完成用户页面顶部优化：

  - 项目名称从 chatgpt2api 改为 AI图片助手
  - 删除顶部 GitHub 链接和 GitHub 图标
  - 删除顶部版本号显示
  - 浏览器页面标题改为 AI图片助手

  修改文件：

  - web/src/components/top-nav.tsx
  - web/src/app/layout.tsx

  保留了内部存储 key 里的 chatgpt2api 命名，避免影响已登录状态和历史记录。
  
  
  7.
  • 已把 声明.txt 的声明内容接入登录页：

  - 进入登录页会先弹出“风险与合规声明”
  - 弹窗没有关闭按钮
  - 用户点击“已阅读知晓”后，才能输入密钥并登录
  - 确认前输入框和登录按钮都是禁用状态

  修改文件：

  - web/src/app/login/page.tsx
  
  8.
  修改UI界面
  
	  
  